'use strict';

describe('myApp.accountRegistration module', function() {

  beforeEach(module('myApp.accountRegistration'));

  describe('accountRegistration controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});